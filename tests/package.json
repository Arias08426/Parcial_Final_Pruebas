{
  "name": "tests",
  "version": "1.0.0",
  "description": "Pruebas E2E para sistema de citas médicas",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
